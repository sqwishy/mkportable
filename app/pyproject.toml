[project]
name = "app"
version = "0.1.0"
description = "Add your description here"
requires-python = "==3.13.*"
dependencies = [
    "django>=6.0",
    "scipy>=1.16.3",
    "scikit-sparse>=0.4.16",
]
